# Changelog

## Unreleased
- Filter enabled affiliate URLs to `http` / `https` before rendering.
- Escape affiliate names and href values before inserting them into HTML.
- Add regression coverage for unsafe schemes while preserving routing behavior.

## 1.0.0
- Initial release.
- Goal, budget and operations-priority routing.
- Affiliate slots disabled by default.
